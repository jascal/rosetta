"""pack.adapters — structured-source → expert package adapters (model-free).

Each adapter turns a machine-readable source (a spec's normative rules, a glossary, …) into the corpus files the rest of
`pack` consumes — no model, no fieldrun. The source's own structure supplies the citations.
"""
